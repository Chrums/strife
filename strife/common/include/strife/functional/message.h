#pragma once

namespace strife {
    namespace functional {
        
        class Message {
            
        public:
        
            Message() = default;
            virtual ~Message() = default;
            
        };

    }
}